#include <iostream>
using namespace std;

int main()
{
    double l, p, q;
    int sum=0;
    cin >> l >> p >> q;
      //l=A+B;
     //l=p*s+q*s;
    //l=s(p+q);
   //s=l/(p+q);
  // A=(p*l)/(p+q);
   cout<<(p*l)/(p+q);
    return 0;
}
